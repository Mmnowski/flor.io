/**
 * Rooms Domain - Plant organization by location
 *
 * Manages user-created rooms for organizing and categorizing plants.
 *
 * Note: Server-only functions are not exported from this barrel to prevent
 * server code from being bundled into the client. Import directly from
 * './rooms.server' in server-only contexts.
 */
