/**
 * Watering Domain - Plant watering tracking and scheduling
 *
 * Records watering events, calculates next watering dates, and identifies plants
 * that need water.
 *
 * Note: Server-only functions are not exported from this barrel to prevent
 * server code from being bundled into the client. Import directly from
 * './watering.server' in server-only contexts.
 */
