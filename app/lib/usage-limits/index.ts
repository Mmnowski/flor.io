/**
 * Usage Limits Domain - Quota tracking and enforcement
 *
 * Tracks monthly AI generation quotas and total plant count limits per user.
 *
 * Note: Server-only functions are not exported from this barrel to prevent
 * server code from being bundled into the client. Import directly from
 * './usage-limits.server' in server-only contexts.
 */
