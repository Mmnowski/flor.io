/**
 * AI Domain - External AI service integrations
 *
 * Provides integrations with third-party AI services for plant identification
 * and care instruction generation.
 *
 * Note: Server-only functions are not exported from this barrel to prevent
 * server code from being bundled into the client. Import directly from
 * './openai.server' or './plantid.server' in server-only contexts.
 */
